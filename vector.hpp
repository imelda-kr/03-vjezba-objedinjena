#ifdef VECTOR_HPP
#define VECTOR_HPP
#include<iostream>
#include<vector>

void unosVek_br(std::vector<int>& v,int broj_el); //unos s brojem elemenata

void unosVek_inter(std::vector<int>& v,int donja_gr,int gornja_gr);  //unos unutar intervala

void ispisVek(std::vector<int>& v,int br_elem);
void ispisVek(std::vector<int>& v);
#endif // VEKTOR_HPP
